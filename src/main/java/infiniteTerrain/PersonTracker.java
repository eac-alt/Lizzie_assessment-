package infiniteTerrain;

import java.awt.List;
import java.util.ArrayList;

import infiniteTerrain.Person;
import infiniteTerrain.Person;
public class PersonTracker {
	
	private List<person> player = new ArrayList<>();
	private Object player;

	public boolean addplayer(Person person) {
		return this.Person.add(person);
	}
	
	public void displayPeople() {
		Object people;
		for (Person p : people) {
			System.out.println(p.getDetails());
		}
	}
	
	public Person findByLocation(String Location) {
		for (Person p : this.people) {
			if (p.getLocation().equals(location)) {
				return p;
			}
		}
		return null;
		
		
		getlocation (); 
		
		// calcs distance from treasure 
		if location == x { 
			System.out.println(" The dial reeds 5 m from");
		
		
			else if choice == n {
				System.out.println("The dial reeds 6m form end ");
				
				else 
			} ("the dial reeds 1m from end")
	}
}
}
